# Validation notes — grounded proposals (v0.1 development)

Corpus content below is paraphrased from the author's private data; every
number and method is real.

**Result: PASSED** (bar: ≥1 kept proposal; result: **2 kept**, 40% hit-rate on
the first-ever batch).

- The kept headline proposal was derived from the cross-tool discovery above:
  the generator turned a "finding here ↔ root cause there, 14 months apart"
  bridge into a concrete implementation plan neither original session reached.
  The full loop — mine → connect → derive → human keeps — worked.
- Grounding fixes validated: theme count consolidated **434 → 85** over a
  richer corpus (1,931 → 3,745 notes) after corpus-scaled clustering and
  message-level noise filtering.
- Guardrails observed working: the novelty gate rejected a real paraphrase;
  feasibility-critic failure degraded to a labeled unknown (never silently
  dropped); every auto-rejection was recorded with a reason.
- Live-only bugs found + fixed: cite-format drift (models return bare ids —
  normalize, drop hallucinated cites), raw excerpts leaking wrapper markup
  into prompts, and rejected proposals permanently consuming their source
  material (now retryable).
- The per-role model map earned its keep same-day: when the primary model's
  daily budget died mid-run, pointing only the generation role at another
  model finished the batch.
